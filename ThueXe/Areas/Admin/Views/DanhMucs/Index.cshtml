@model IEnumerable<WebCar.Areas.Admin.Models.Entities.DanhMuc>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameCategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCategory)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DanhMucId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DanhMucId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DanhMucId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
